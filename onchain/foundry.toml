[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
script = "script"
solc = "0.8.25"
optimizer = true
optimizer_runs = 200

# No external dependencies beyond forge-std (used only by the test + deploy
# script). AllocationDisplay.sol itself is zero-dependency.
