# The Mystic allocation demo loads all data from /data/*.json at runtime
# (synced by sync-data.mjs). It does not broadcast, so no RPC or key is needed.
# This file is intentionally empty of secrets.
