# The simulated-enclave model service (signs plans via the real TEE in TEE mode).
# Build context: demo/tee-model
FROM node:22-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install --no-audit --no-fund
COPY . .
ENV PORT=8080
EXPOSE 8080
CMD ["node","--import","tsx","src/index.ts","serve"]
