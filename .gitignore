# ---- secrets: NEVER commit ----
**/.env
**/.env.local
**/.env.*.local

# ---- the real TEE stack (Flare's tee-node), if present locally ----
# Its own repos + a local .env holding a private key. Never republish it here.
TEE/

# ---- dependencies / build artifacts ----
# Note: contracts/lib (vendored forge-std + OpenZeppelin, MIT) IS committed so
# `forge build` works from a clean checkout with no extra install step.
**/node_modules/
**/dist/
**/*.tsbuildinfo
contracts/out/
contracts/cache/
contracts/broadcast/
frontend/public/data/

# ---- misc ----
**/*.log
tee-model/last-plan.json
.DS_Store
