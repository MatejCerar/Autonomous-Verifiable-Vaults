# dependencies
node_modules/
tee-extension/**/node_modules/

# build output
dist/
*.tsbuildinfo

# foundry build artifacts (contracts/lib is vendored and IS committed)
contracts/out/
contracts/cache/
contracts/broadcast/

# secrets / env
.env
.env.*
!.env.example

# local deploy state (addresses are printed by the deploy script)
deployed.coston2.json

# os / editor
.DS_Store
